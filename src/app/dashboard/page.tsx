// the main dashboard page — shows the timeline view by default
// actual view switching happens client-side via the DashboardContent component

import { DashboardContent } from '@/components/dashboard/DashboardContent'

export default function DashboardPage() {
  return <DashboardContent />
}
